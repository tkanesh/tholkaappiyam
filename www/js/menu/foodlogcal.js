$(document).on("pageshow", "#foodLogCal", function() {
    /* var foodLog = window.localStorage.getItem("foodLog") + ""; */

    /* generateLog(foodLog); */
    
    /* window.onload = function() { */
//        Calendar.setup({
//            dateField     : 'embeddedDateField',
//            parentElement : 'embeddedCalendar'
//        })
        /* } */

});
